# pytest.ini
[pytest]
pythonpath = .
markers =
    extraction: marks a test that tests data extraction
    transformation: marks a test that tests data transformation
    loading: marks a test that tests loading data into a database
    flask: marks a test that tests Flask application routes
    utils: marks a test that tests utility functions
